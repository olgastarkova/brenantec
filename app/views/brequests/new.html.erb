<%= simple_form_for [@workshop, @brequest] do |f| %>
	<%= f.association :date, collection: @sessions, include_blank: true %>
	<%= f.input :name %>
	<%= f.input :email %>
	<%= f.input :message %>
	<%= f.submit "Envoyer la demande", class: "btn btn-primary-rose" %>
<% end %>