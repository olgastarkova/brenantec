<div class="container">
	<div class="row">
		<div class="col-xs-12 col-sm-4 col-sm-offset-4">
			<%= simple_form_for @coach  do |f| %>
				<%= f.input :name %>
				<%= f.input :role, placeholder: "Prof certifié de Yoga" %>
				<%= f.input :intro %>
				<%= f.submit "Enregistrer", class: "btn btn-primary" %>
			<% end %>
		</div>
	</div>
</div>
