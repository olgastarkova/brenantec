<div class="container">
  <div class="row">
    <div class="col-xs-12 col-sm-8 col-sm-offset-2">
      <div class="test-box">
        <div class="poem-author-pic">
          <% if @coach.photo? %>
            <%= cl_image_tag @coach.photo.path, width: 200, height: 200, crop: :fill, gravity: :face %>
          <% else %>
            <%= image_tag 'http://placehold.it/180x180', class: "text-center" %>
          <% end %>
        </div>
        <div class="box-title">
          <h2><%= @coach.name %></h2>
          <h6 class="customized"><%= @coach.role %></h6>
        </div>
      </div>
      
      <p class="space-before">
        <%= simple_format(@coach.intro) %>
      </p>
      <%= link_to 'Revenir en arrière', url_for(:back) %>
    </div>
  </div>
</div>
